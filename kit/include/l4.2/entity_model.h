// Модель сущностей. Занятия 3.2 и 4.1.
//
// РЕАЛИЗАЦИЯ ВЫДАНА ГОТОВОЙ — `tasks/3.2/handout/entity_model.cpp`, копируется
// в `src/`. Занятие 3.2 про стирание типа, и перебор двух векторов к нему
// отношения не имеет; вашей работой остаётся подключение модели, а с занятия
// 3.3 — и правка самого файла. Подробности — в README рядом с выдачей.
//
// До этого занятия агент помнил только последние события — кольцевой буфер
// на 256 мест. Этого хватает, чтобы ответить «что было секунду назад»,
// и не хватает ни на один вопрос про связи: кто чей родитель, кто записал
// этот файл, откуда взялся этот процесс.
//
// Вопросы эти не выдуманные. Детект без цепочки предков бесполезен: аналитик
// видит «wscript.exe вышел в сеть» и первым делом спрашивает, кто его запустил.
// А реагирование на занятии 3.3 без модели вообще невозможно — убить надо
// поддерево процессов, а не один процесс, и изолировать файл-источник,
// а не тот, который попался в событии.
//
// ЧТО ИЗМЕНИЛОСЬ НА ЗАНЯТИИ 4.1
//
// Три вещи, и все три — следствия одного замера.
//
//   личность процесса        ключ записи — пара (pid, start), а не номер.
//                            До 4.1 `process_start` с уже известным номером
//                            затирал прежнюю запись вместе с её цепочкой;
//   хранилище                словари вместо векторов с линейным поиском;
//   вытеснение               по возрасту и по объёму, на тике os_wait,
//                            и об этом печатается в сводке.
//
// Первое стало возможным из-за второго: пара как ключ требует словаря
// и специализации `std::hash`, а словарей до лекции 13 не было. Третье стало
// НЕОБХОДИМЫМ из-за первого: пока номер затирал номер, размер модели был
// ограничен числом номеров сам собой. Теперь каждое новое воплощение — новая
// запись, и на нагрузочном журнале их сто шестьдесят тысяч вместо четырёх.
//
// Это стоит заметить отдельно. Исправление ошибки увеличило потребление
// памяти. Так бывает часто, и «зато правильно» здесь не ответ: агент, съевший
// память хоста, защищает его хуже, чем агент с путаницей в номерах. Отсюда
// и требование занятия — вытеснение, а не «предел на всякий случай».
//
// ЧТО МОДЕЛЬ ЗНАЕТ И ЧЕГО НЕ ЗНАЕТ
//
// Только то, что видела в потоке. Процессы, существовавшие до подписки,
// известны ей ровно настолько, насколько попали в события: pid встретился —
// запись появилась, а image и ppid остались пустыми, потому что их никто
// не сообщал. Поэтому цепочка предков обрывается там, где обрывается знание
// потока, и в выводе это видно: `880>1042(cmd.exe)`.
//
// Спросить у системы (`os_process_info`) модель не пытается, и это выбор,
// а не упущение: у обращений к границе есть бюджет (`max_os_queries`),
// и тратить его на историю — решение занятия 3.3, где появится реагирование
// и обращения к границе станут неизбежны.
//
// ПОРЯДКА У СЛОВАРЯ НЕТ, И ЭТО ВИДНО СНАРУЖИ
//
// Самая неприятная часть перехода на `unordered_map`, и заметить её надо
// до первого прогона, а не после. Обход хеш-таблицы идёт в порядке, который
// стандарт не определяет: он зависит от реализации, от числа корзин, от
// истории вставок. Значит любой результат, который получается обходом
// и уходит наружу — в вывод, в план реагирования, в эталон, — обязан быть
// упорядочен явно.
//
// В этом заголовке таких функций три: GetProcessTree, GetFilesCreatedBy
// и FindSpawnSource. У всех трёх в описании сказано, по чему именно они
// упорядочены. Это не украшение подписи, это часть контракта: эталоны
// сценариев сравниваются побайтово, и агент, у которого поддерево на MSVC
// и на g++ выходит в разном порядке, эталоном не проверяется вообще.

#ifndef NANO_EDR_KIT_ENTITY_MODEL_H
#define NANO_EDR_KIT_ENTITY_MODEL_H

#include <cstddef>
#include <cstdint>
#include <functional>
#include <string>
#include <unordered_map>
#include <vector>

#include "event.h"

namespace nano_edr {

// Личность процесса.
//
// Номер сам по себе личностью не является: система выдаёт его из ограниченного
// диапазона и после завершения процесса выдаёт снова. На живой машине это
// происходит за часы, в нагрузочном журнале — за минуты, а в сценарии
// `pid_reuse` — за две секунды, и там номер 1042 успевает принадлежать
// скриптовому хосту и блокноту пользователя.
//
// Личность — пара «номер и время старта». Ровно её же требует граница:
// `os_process_kill` принимает `expected_start_time` и отвечает
// `OS_PID_REUSED`, если номер уже не тот. То есть контракт os.h с самого
// занятия 2.1 говорил, что личность составная, — а модель до 4.1 этого
// не умела выразить, потому что для составного ключа нужен словарь.
//
// start.ms == 0 означает «воплощение неизвестно»: процесс попал в модель
// не через `process_start`, а потому что упомянут в каком-то событии. Такие
// записи полезны (цепочка предков доходит до них) и остаются отдельными
// от записей с известным временем старта.
struct ProcessKey {
    std::string pid;
    Timestamp start;

    // Сравнение по обоим полям сразу. `= default` (лекция 8) генерирует
    // почленное сравнение — писать его руками здесь было бы ровно тем же
    // текстом с возможностью опечатки.
    bool operator==(const ProcessKey& other) const = default;
};

}  // namespace nano_edr

// Специализация std::hash для своего типа.
//
// Ровно так, как показывает лекция 13, и с тем же обязательным `noexcept`:
// стандарт требует, чтобы хеш не бросал. Требование не формальность —
// `unordered_map` вызывает его в местах, где раскрутка стека оставила бы
// таблицу в половинчатом состоянии.
//
// Комбинирование двух хешей — сдвигом и `^`. Это не лучший из известных
// способов (у boost::hash_combine перемешивание сильнее), но его хватает:
// pid и время старта независимы, коллизии здесь берутся не из плохого
// перемешивания, а из того, что номеров всего шестьдесят пять тысяч.
//
// Тела здесь нет намеренно — как и у всего остального в заготовках. Определение
// пишете вы, в своём .cpp:
//
//   std::size_t std::hash<nano_edr::ProcessKey>::operator()(
//           const nano_edr::ProcessKey& key) const noexcept { ... }
template <>
struct std::hash<nano_edr::ProcessKey> {
    std::size_t operator()(const nano_edr::ProcessKey& key) const noexcept;
};

namespace nano_edr {

// Процесс, как его видел поток.
struct ProcessRecord {
    std::string pid;
    std::string ppid;   // пусто, если процесс появился не через process_start
    std::string image;  // пусто по той же причине
    std::string cmdline;
    Timestamp start;
    bool alive = true;

    // ЗАНЯТИЕ 4.1. Когда запись последний раз упоминалась в потоке. По этому
    // полю идёт вытеснение по возрасту — не по времени старта: процесс,
    // запущенный час назад и работающий сейчас, свежий, а не старый.
    Timestamp last_seen;

    // Поля `parent` здесь нет намеренно, и это стоит объяснить, потому что
    // первое решение было именно таким — запомнить ключ родителя в момент
    // старта.
    //
    // Оно неверно, и ломается на первом же сценарии курса. В phishing_macro
    // событие `process_start pid=1042 ppid=880` приходит раньше, чем модель
    // впервые слышит про 880: тот появляется в потоке секундой позже, своей
    // записью в файл. В момент старта родителя ещё не существовало, и ключ
    // был бы пустым навсегда — цепочка предков потеряла бы корень.
    //
    // Поэтому родитель разрешается на вопрос, а не на событие: см. ParentKey
    // и KeyAt. Знание модели растёт, и ответ на «кто родитель» обязан расти
    // вместе с ним.
};

// Файл, как его видел поток.
struct FileRecord {
    std::string path;
    std::string creator_pid;  // file_create или file_move в этот путь
    std::string writer_pid;   // кто записал последним
    Timestamp last_write;
    bool deleted = false;

    // ЗАНЯТИЕ 4.1. Когда путь последний раз упоминался в потоке — любым
    // событием, не только записью.
    //
    // Отдельное поле, а не last_write, и разница не формальная. Путь может
    // попасть в модель удалением или переименованием: тогда про запись
    // в него ничего не известно, и last_write остаётся нулём. Вытеснение
    // по last_write выбросило бы такую запись на первом же тике — она
    // выглядела бы старой на пятьдесят пять лет.
    //
    // Ошибка эта не гипотетическая: на сценарии ransomware так вытеснялся
    // ровно один файл, и найти это можно было только по строке в сводке.
    Timestamp last_seen;

    // ЗАНЯТИЕ 3.3. Путь появился переименованием, а не записью.
    //
    // Различие выглядит мелким и стоит сорока файлов данных. Шифровальщик
    // создаёт путь `quarterly.xlsx.locked` — создаёт по-настоящему, этого
    // пути раньше не было, и создатель у него он. Реагирование, которое
    // удаляет всё созданное процессом, удалит документы жертвы.
    //
    // Переименованное — это чьи-то данные под другим именем. Принесённое —
    // это груз атакующего. Удалять можно второе.
    bool from_move = false;
};

// Пределы хранилища. Занятие 4.1.
//
// Две границы одновременно, как требует лекция 13, и по отдельности каждая
// дырявая. Только возраст не спасает от всплеска: за минуту может прийти
// миллион событий, и окно в пять минут честно удержит их все. Только объём
// не спасает от медленного накопления: предел будет достигнут и память
// останется занятой навсегда, даже если полезны последние сто записей.
//
// Значения приходят от сервера через `edr_config_get`. Умолчания здесь —
// не «правильные числа», а числа, при которых агент не падает без
// конфигурации; настоящие подбираются замером на нагрузочном журнале.
struct EvictionLimits {
    std::size_t max_processes = 4096;
    std::size_t max_files = 8192;

    // Возраст в миллисекундах МОДЕЛЬНОГО времени — того, что в событиях.
    // Не системных часов: тогда на журнале, проигранном за секунду, не
    // вытеснилось бы ничего, а тесты зависели бы от скорости машины.
    uint64_t max_idle_ms = 300000;
};

// Сколько записей выброшено. Занятие 4.1.
//
// Считается и печатается по тому же правилу, что подавленные дубликаты
// на 3.2 и свои события на 3.3: молча выбрасывать нельзя даже мусор.
// Ноль здесь на нагрузочном журнале означает, что вытеснение не работает.
struct EvictionStats {
    std::size_t processes_by_age = 0;
    std::size_t processes_by_size = 0;
    std::size_t files_by_age = 0;
    std::size_t files_by_size = 0;

    std::size_t total() const {
        return processes_by_age + processes_by_size + files_by_age +
               files_by_size;
    }
};

class EntityModel {
 public:
    // Обновить модель событием. Всё, что модель узнаёт, она узнаёт здесь.
    void Observe(const Event& event);

    // --- пределы и вытеснение (занятие 4.1) ---

    void SetLimits(const EvictionLimits& limits);
    const EvictionLimits& limits() const;

    // Выбросить старое и лишнее. Зовётся на тике os_wait, а не на событии:
    // проход по всей модели на каждом событии стоил бы больше, чем экономит,
    // а тик — это ровно «сейчас есть время».
    //
    // «Сейчас» берётся из последнего наблюдённого события, а не из системных
    // часов, — см. EvictionLimits. На пустой модели не делает ничего.
    void Evict();

    const EvictionStats& eviction() const;

    // Время последнего наблюдённого события. Модельное «сейчас».
    Timestamp last_seen() const;

    // --- точечный доступ ---
    // Возвращают nullptr, если такого нет. Особое значение, а не исключение:
    // «модель про это не знает» — штатный исход, а не отказ. Тот же выбор,
    // что у FindField на занятии 1.3, и по той же причине.

    // Нынешний носитель номера — то есть последнее воплощение, которое модель
    // видела. Именно это нужно почти везде: событие приносит номер, а не пару,
    // и относится оно к тому, кто держит номер сейчас.
    const ProcessRecord* FindProcess(const std::string& pid) const;

    // ЗАНЯТИЕ 4.1. Конкретное воплощение. Нужно там, где номера уже
    // недостаточно: цепочка предков идёт по ключам, а не по номерам.
    const ProcessRecord* FindProcess(const ProcessKey& key) const;

    // Ключ нынешнего носителя номера — то есть последнего воплощения.
    // Пустой pid, если про номер ничего не известно.
    ProcessKey CurrentKey(const std::string& pid) const;

    // ЗАНЯТИЕ 4.1. Кто держал номер в указанный момент: последнее воплощение,
    // начавшееся не позже `when`.
    //
    // Это и есть ответ на переиспользование номера, и он один на все вопросы
    // про связи. Событие говорит «родитель 1042»; какой из двух процессов
    // с номером 1042 имелся в виду, зависит от того, когда это было сказано.
    //
    // Записи с неизвестным временем старта (`start.ms == 0`) подходят всегда:
    // «модель не знает, когда он начался» — не повод считать, что он начался
    // позже. Именно так находится родитель, про которого модель узнала позже
    // ребёнка.
    ProcessKey KeyAt(const std::string& pid, Timestamp when) const;

    // Ключ родителя этого воплощения. Пустой pid, если родитель неизвестен:
    // либо процесс появился не через process_start, либо про его ppid
    // не было ни одного события.
    ProcessKey ParentKey(const ProcessKey& child) const;

    const FileRecord* FindFile(const std::string& path) const;

    // --- запросы по связям ---

    // Цепочка предков: сам процесс, затем родитель, затем его родитель.
    // Обрывается там, где кончается знание модели. Пустой вектор, если про pid
    // ничего не известно.
    //
    // ЗАНЯТИЕ 4.1. Внутри обход идёт по ключам родителей, а не по номерам,
    // поэтому переиспользованный номер больше не подменяет предка. Наружу
    // по-прежнему отдаются номера: цепочка нужна для печати и для связки
    // `child_of_prev`, а там номер и есть то, что сравнивают.
    std::vector<std::string> GetAncestorChain(const std::string& pid) const;

    // Поддерево процессов: сам процесс и все его потомки. То, что убивает
    // реагирование на занятии 3.3.
    //
    // Порядок: обход в ширину, внутри уровня — по времени старта, при равном
    // времени — по номеру. Порядок задан в контракте, потому что из него
    // получается порядок действий в плане, а тот сравнивается с эталоном.
    // Обход словаря сам по себе порядка не имеет, см. заголовок.
    std::vector<std::string> GetProcessTree(const std::string& pid) const;

    // Пути файлов, созданных этим процессом. Упорядочены по пути.
    //
    // ЗАНЯТИЕ 3.3. Параметр brought_only отбирает только принесённое: файлы,
    // появившиеся записью, а не переименованием. Умолчание false оставлено
    // ради тестов занятия 3.2, которые эту функцию уже проверяют; всё, что
    // строит план реагирования, передаёт true и обязано передавать true.
    std::vector<std::string> GetFilesCreatedBy(const std::string& pid,
                                               bool brought_only = false) const;

    // Файл, из-за которого процесс появился.
    //
    // Связка `path_in_cmdline`: среди известных модели файлов ищется тот,
    // чей путь встречается в командной строке процесса. Именно так виден
    // дроппер: `cmd.exe` пишет `%TEMP%\a.js`, затем запускается
    // `wscript.exe C:\...\Temp\a.js`, и связь между двумя событиями — путь.
    //
    // Из подходящих выбирается записанный последним; при равном времени
    // записи — меньший по пути. Второе условие нужно не по смыслу, а из-за
    // словаря: без него результат зависел бы от порядка обхода таблицы.
    //
    // Возвращает nullptr, если связь не нашлась. Так и должно быть в
    // большинстве случаев: у обычного процесса в командной строке нет файла,
    // который кто-то только что записал.
    const FileRecord* FindSpawnSource(const std::string& pid) const;

    // Цепочка предков строкой для вывода и для детекта:
    // `880>1042(cmd.exe)>1101(wscript.exe)`, старший первым. Пусто, если
    // про процесс ничего не известно.
    std::string DescribeChain(const std::string& pid) const;

    std::size_t process_count() const;
    std::size_t file_count() const;

 private:
    ProcessRecord* MutableProcess(const std::string& pid, Timestamp now);

    // Завести запись под конкретным ключом и вписать её в индекс воплощений.
    ProcessRecord* NewProcess(const ProcessKey& key, Timestamp now);

    FileRecord* MutableFile(const std::string& path, Timestamp now);

    // created — путь появился этим событием; from_move — появился
    // переименованием. Второе имеет смысл только вместе с первым.
    void NoteFileWrite(const Event& event, const std::string& path,
                       bool created, bool from_move = false);

    // Перестроить индекс воплощений по таблице процессов. Зовётся после
    // вытеснения: индекс обязан жить ровно столько, сколько то, на что
    // он указывает.
    void RebuildIndex();

    // ЗАНЯТИЕ 4.1. Хранилище — словари.
    //
    // processes_ по ключу-паре: все воплощения, которые модель помнит.
    //
    // incarnations_ — индекс «номер -> времена старта, по возрастанию».
    // Он нужен по двум причинам, и обе существенные. Первая: события приносят
    // номер, а не пару, и без индекса на каждом событии пришлось бы искать
    // по всей таблице — ровно то, от чего занятие уходит. Вторая: чтобы
    // ответить, кто держал номер в такой-то момент, нужны все воплощения
    // этого номера, а не только последнее.
    std::unordered_map<ProcessKey, ProcessRecord> processes_;
    std::unordered_map<std::string, std::vector<Timestamp>> incarnations_;
    std::unordered_map<std::string, FileRecord> files_;

    EvictionLimits limits_;
    EvictionStats eviction_;
    Timestamp last_seen_;

    // Когда последний раз шёл проход по возрасту. Модельное время, как и всё
    // остальное здесь. Проход дорог, а находит он редко, — см. Evict.
    Timestamp last_age_sweep_;
};

}  // namespace nano_edr

#endif  // NANO_EDR_KIT_ENTITY_MODEL_H
