// Точка входа doctest. Вынесена в отдельную единицу трансляции, чтобы
// тяжёлый заголовок разбирался один раз, а не при каждой правке тестов.

#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include "doctest.h"
