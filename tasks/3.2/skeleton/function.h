// Заготовка своего Function. Занятие 3.2.
//
// Скопируйте файл в `src/` и допишите. С этого момента он ваш: правьте что
// угодно, включая этот комментарий.
//
// ЧТО ЗДЕСЬ ЛЕЖИТ
//
// Публичный интерфейс целиком и ни одного тела. Интерфейс менять нельзя —
// против него собраны выданные тесты и выданные заготовки `alert_sink.h`
// и `rule.h`. Приватной части здесь нет, и это задание: как хранить
// вызываемый объект, не зная его типа, — то, ради чего занятие и нужно.
//
// ЗАДАНИЕ
//
// Сложить в переменную одного типа любой вызываемый объект с подходящей
// подписью — свободную функцию, лямбду с захватом, объект с `operator()` —
// и вызвать его, ничего не зная о его типе. Как это устроено внутри
// и что за это платится, разобрано в постановке занятия; три свойства,
// которые обязаны получиться, проверяются выданными тестами.
//
// Тела пишутся прямо здесь: шаблон нельзя разложить на заголовок и `.cpp` —
// компилятору нужно тело в той единице трансляции, где шаблон используется.

#ifndef NANO_EDR_FUNCTION_H
#define NANO_EDR_FUNCTION_H

#include <memory>
#include <utility>

namespace nano_edr {

// Объявлен, но не определён — как `FieldTraits` на занятии 3.1. Определена
// только частичная специализация ниже, и это заставляет писать `Function`
// с подписью в скобках: `Function<void(const Detection&)>`. Написать
// `Function<int>` не получится, и это правильно — такой тип бессмыслен.
template <typename Signature>
class Function;

template <typename R, typename... Args>
class Function<R(Args...)> {
 public:
    Function();

    // Ловит любой вызываемый объект. Шаблонный конструктор, а не конструктор
    // от конкретного типа: типов бесконечно много, и в этом весь смысл.
    template <typename F>
    Function(F callable);  // NOLINT

    // `const` здесь про сам Function, а не про то, что внутри.
    R operator()(Args... args) const;

    // Пустой Function вызывать нельзя. Проверять до вызова — дело
    // вызывающего, отсюда явное преобразование к bool.
    explicit operator bool() const;
};

}  // namespace nano_edr

#endif  // NANO_EDR_FUNCTION_H
