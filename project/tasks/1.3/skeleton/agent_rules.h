// Заготовка доступа к таблице правил агента. Занятие 1.3.
//
// Скопируйте файл в `src/` — с этого момента он ваш. Дописывать в него можно
// что угодно: объявления самих правил, если хотите проверять их поштучно
// своим тестом, вспомогательные функции, всё прочее. Убирать нельзя только
// две функции ниже — против них собраны выданные тесты.
//
// ЧТО ЗДЕСЬ ЛЕЖИТ
//
// Два объявления и ни одного тела. Сами правила пишете вы: пять функций
// и constexpr-таблица, которая связывает идентификатор, важность
// и функцию-проверку. Какие это правила и по какому условию каждое
// срабатывает — в постановке занятия, раздел «Пять правил».
//
// ПОЧЕМУ ЭТО ВООБЩЕ ВЫДАНО
//
// Таблица нужна не одному вашему main: через эти две функции до неё
// добираются выданные тесты. Где лежит ваш файл с правилами и как называются
// ваши функции-проверки, они не знают и знать не должны — иначе тест начал бы
// диктовать раскладку вашего кода.
//
// А выдано заготовкой, а не комплектом, потому что файл вы правите: на
// следующих занятиях набор правил меняет форму, и объявление меняется вместе
// с ним. Комплект менять нельзя, поэтому всё, что правится, живёт в `src/`.

#ifndef NANO_EDR_AGENT_RULES_H
#define NANO_EDR_AGENT_RULES_H

#include "rules.h"

namespace nano_edr {

// Таблица правил агента и её длина — ровно то, что принимает CheckRules.
const Rule* AgentRules();
size_t AgentRuleCount();

}  // namespace nano_edr

#endif  // NANO_EDR_AGENT_RULES_H
