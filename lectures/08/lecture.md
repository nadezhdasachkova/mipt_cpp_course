# Лекция 8. Шаблоны: введение

> Конспект к лекции для самостоятельного чтения. Слайды (`slides.md`) повторяют структуру, конспект подробнее объясняет «почему».

## 0. О чём эта лекция

Шаблоны — одна из самых **мощных** и одновременно одна из самых **сложных** частей C++. На них построены `std::vector`, `std::map`, `std::function`, `std::optional`, алгоритмы STL, и почти весь современный generic C++. Без шаблонов невозможно писать обобщённый код, который работает с разными типами без потери производительности.

Сегодня мы возьмём основу — синтаксис, модель компиляции, перегрузку, специализацию, non-type параметры, зависимые имена. На следующей лекции добавим variadic templates и fold expressions. Что лежит глубже — SFINAE, концепты, ranges, метапрограммирование — в курс не входит.

Подача намеренно практическая: доказывать Тьюринг-полноту системы типов здесь не нужно (она действительно такая, это просто следствие). Важнее показать, что и как пишут в реальном коде.

## 1. Зачем нужны шаблоны

В C, если нужна одна и та же функция для разных типов, приходится либо дублировать код, либо использовать макросы:

```cpp
int max_int(int a, int b)       { return a > b ? a : b; }
double max_d(double a, double b){ return a > b ? a : b; }
```

С шаблонами один код работает с любым типом:

```cpp
template <typename T>
T max_val(T a, T b) { return a > b ? a : b; }

max_val(3, 5);       // T = int
max_val(3.0, 5.0);   // T = double
max_val(std::string{"a"}, std::string{"b"});   // T = std::string
```

Важно понимать: шаблоны — это **не runtime-механизм**. Они не делают полиморфизм через [виртуальные функции](../glossary.md#virtual-function) (virtual functions) или интерпретатор. Шаблоны — это **инструкции компилятору**: «когда тебе понадобится `max_val` для типа `T`, сгенерируй её вот так». Это происходит на этапе **компиляции**.

Порождение функции под конкретный `T` называется **[инстанцирование](../glossary.md#instantiation) шаблона** (template instantiation):

![Слева один шаблон max_val в исходнике и три вызова с разными аргументами, розовыми стрелками — три отдельные функции в бинарнике: int max_val(int, int), double max_val(double, double), string max_val(string, string)](img/instantiation.svg)

Преимущества:

- **Нулевые накладные расходы.** Никаких виртуальных вызовов, никакого [type erasure](../glossary.md#type-erasure) в рантайме.
- **Compile-time проверка типов.** Если `T` не поддерживает оператор `>` — ошибка на этапе компиляции, а не runtime exception.
- **Один код для всех типов.** DRY на уровне языка.

Цена:

- **Больше времени компиляции** — компилятор реально генерирует код для каждого использования.
- **Бóльший бинарник** — несколько инстанцирований одного шаблона = несколько функций в бинарнике.
- **Сложные ошибки.** Если шаблон используется неправильно, ошибки могут быть очень длинными (об этом — в разделе про [концепты](../glossary.md#concept) (concepts)).

## 2. Шаблонные функции

Базовый синтаксис:

```cpp
template <typename T>
T add(T a, T b) {
    return a + b;
}
```

`template <typename T>` объявляет шаблонный параметр-тип. `typename` — ключевое слово, говорящее «параметр это тип». Существует исторический синоним `class T` — означает ровно то же самое (название обманчивое, `T` не обязан быть классом).

Использование:

```cpp
add(2, 3);              // T = int, выведено из аргументов
add<double>(2, 3);      // T = double, указали явно (int преобразуется)
add<int>(2.0, 3.0);     // T = int, double обрежется до int
```

**[Вывод типов](../glossary.md#deduction)** (template argument deduction): компилятор определяет `T` по аргументам, если может. Если нет — нужно указать явно через `<...>`.

### Несколько параметров

```cpp
template <typename T, typename U>
auto pair_sum(T a, U b) {
    return a + b;
}

pair_sum(1, 2.5);   // T = int, U = double, результат — double
```

Результат `double` дают [обычные арифметические преобразования](../glossary.md#usual-arithmetic-conversions) (usual arithmetic conversions) для `int + double`.

Можно использовать тот же параметр несколько раз — это значит «все эти места должны быть одного типа»:

```cpp
template <typename T>
void swap_(T& a, T& b) {
    T tmp = a; a = b; b = tmp;
}
```

При вызове `swap_(x, y)`, где `x` объявлен как `int`, а `y` как `double`, будет [CE](../glossary.md#ce) (compilation error): компилятор не может одновременно вывести `T = int` (из первого аргумента) и `T = double` (из второго). Литералы `swap_(1, 2.0)` не подошли бы и по другой причине — `T&` не привязывается к временному значению.

### Когда нужно указать тип явно

Когда тип не выводится из аргументов:

```cpp
template <typename T>
T parse_number(const std::string& s);

parse_number<int>("42");      // ok, T = int
parse_number("42");           // CE: T не выводится
```

`T` здесь только в возвращаемом типе. Из аргументов вывести нельзя, поэтому указываем явно.

Также — когда хотим принудительно использовать конкретный тип, отличный от выведенного.

### И кстати про `sizeof`

Теперь видно, где `sizeof` из лекции 2 по-настоящему нужен. В обычном коде размер типа известен заранее, и спрашивать его не у кого — вы его и так знаете. В шаблоне тип неизвестен до инстанцирования, и `sizeof(T)` — единственный способ о нём спросить:

```cpp
template <typename T>
void info() {
    std::print("size = {}\n", sizeof(T));
}

info<int>();        // size = 4
info<double>();     // size = 8
```

Значение известно на этапе компиляции, поэтому `sizeof(T)` годится и как условие, по которому шаблон выбирает поведение: например, «маленькие типы передаём по значению, большие — по `const&`».

## 3. Шаблонные классы

```cpp
template <typename T>
class Stack {
public:
    void push(const T& x) { data_.push_back(x); }
    T pop() { T top = data_.back(); data_.pop_back(); return top; }
    size_t size() const { return data_.size(); }
private:
    std::vector<T> data_;
};

Stack<int> s_int;
Stack<std::string> s_str;
```

Несколько важных замечаний.

**`Stack<int>` и `Stack<std::string>` — это разные типы.** Они не имеют общего родителя. Нельзя написать `Stack<int>& ref = s_str;` — это разные классы, никак не связанные. Поэтому шаблоны называют **compile-time полиморфизмом**: общее «поведение» определено в шаблоне, но в рантайме каждый набор параметров — отдельный тип.

### Методы вне класса

Если определяете метод **снаружи** класса — нужно повторить шаблонную часть:

```cpp
template <typename T>
class Stack {
public:
    void push(const T& x);
};

template <typename T>            // ← здесь повторяем шаблонную часть
void Stack<T>::push(const T& x) {
    data_.push_back(x);
}
```

Имя метода — `Stack<T>::push`. То есть класс — `Stack<T>` (с указанием параметра), затем `::push`. Если параметров несколько — все нужны: `Pair<T, U>::set`.

Это синтаксически громоздко, и поэтому многие методы шаблонных классов определяются прямо внутри класса (это автоматически делает их `inline`).

## 4. CTAD — вывод аргументов шаблона класса (C++17)

Заметьте асимметрию: для **функций** компилятор выводит `T` из аргументов автоматически (`add(2, 3)` — и он понял, что `T = int`), а для **классов** до C++17 приходилось писать аргументы шаблона руками:

```cpp
std::vector<int> v{1, 2, 3};            // <int> обязательно
std::pair<int, double> p{1, 2.0};       // <int, double> обязательно
```

Это выглядело непоследовательно: информации для вывода достаточно (аргументы конструктора её содержат), но язык ей не пользовался. C++17 эту асимметрию убрал — появился **[CTAD](../glossary.md#ctad)** (class template argument deduction) — вывод аргументов шаблона класса:

```cpp
std::vector v{1, 2, 3};                 // CTAD: vector<int>
std::pair  p{1, 2.0};                   // CTAD: pair<int, double>
std::tuple t{1, 2.0, "x"};              // tuple<int, double, const char*>
std::array a{1, 2, 3};                  // array<int, 3> — и размер тоже выведен
```

### Как это работает

Компилятор берёт все конструкторы шаблонного класса и строит из них **неявные [deduction guides](../glossary.md#deduction-guide)** — набор «функций-кандидатов», по которым и происходит вывод аргументов ровно так же, как для обычной шаблонной функции. Для большинства классов этого достаточно, и ничего писать не нужно:

```cpp
template <typename T>
struct Wrapper {
    T value;
};

Wrapper w{42};        // CTAD: Wrapper<int>
```

### Пользовательские deduction guides

Иногда параметр класса **не выводится** из аргументов конструктора напрямую. Классический случай — конструктор принимает пару итераторов, а параметр класса — тип элемента:

```cpp
template <typename T>
class Container {
public:
    template <typename It>
    Container(It begin, It end);        // T нигде не упомянут!
    // ...
};
```

Из `Container(v.begin(), v.end())` компилятор не может вывести `T` — в конструкторе есть только `It`. Тогда пишут **явный guide**:

```cpp
template <typename It>
Container(It, It) -> Container<typename std::iterator_traits<It>::value_type>;

std::vector<int> v;
Container c(v.begin(), v.end());        // Container<int>
```

Синтаксис читается так: «если конструктор вызван с двумя аргументами типа `It`, то класс — `Container<value_type от It>`». Именно так устроены guides у стандартных контейнеров.

Пользовательские deduction guides — инструмент **авторов библиотек**. В прикладном коде они почти не встречаются: там достаточно неявных. Знать полезно, чтобы понимать чужой код и не пугаться странного синтаксиса со стрелкой.

### Когда CTAD не срабатывает

Пара практических оговорок:

- **Нельзя вывести часть аргументов.** Либо все, либо ни один: `std::pair<int> p{1, 2.0};` — CE.
- **Не работает для [шаблонов псевдонимов](../glossary.md#type-alias) (alias templates)** (до C++20). `template <typename T> using Vec = std::vector<T>;` — `Vec v{1,2,3}` до C++20 не компилировалось.
- **Осторожно с копированием.** `std::vector v2{v1};` даст не `vector<vector<int>>`, а копию `v1` — сработает [копирующий конструктор](../glossary.md#copy-constructor) (copy constructor), а не конструктор от initializer_list. Это редкий, но неприятный сюрприз.

## 5. Шаблоны псевдонимов и переменных

### Шаблоны псевдонимов (C++11)

Можно создавать «сокращения» для шаблонных типов:

```cpp
template <typename T>
using Vec = std::vector<T>;

Vec<int> v;            // эквивалент std::vector<int>
```

Полезно для:

- Сокращения длинных имён (`std::vector<std::pair<std::string, int>>` → `Pairs`)
- Связывания нескольких типов в один псевдоним
- В [type traits](../glossary.md#type-traits) — почти все хелперы в стандартной библиотеке имеют форму `*_t`:
  ```cpp
  // в стандарте:
  template <typename T>
  using remove_const_t = typename remove_const<T>::type;
  ```

### Шаблоны переменных (C++14)

```cpp
template <typename T>
constexpr T pi = T(3.1415926535897932385L);

double d = pi<double>;       // 3.14... в double
float  f = pi<float>;        // в float (с потерей точности)
```

Это **[шаблон переменной](../glossary.md#variable-template)** (variable template): переменная, тип и значение которой зависят от параметра. Здесь она `constexpr`, но шаблон переменной не обязан быть константой. В стандарте такая форма используется для `*_v` хелперов:

```cpp
// в стандарте:
template <typename T, typename U>
constexpr bool is_same_v = is_same<T, U>::value;
```

С этим вы будете встречаться постоянно при работе с type traits.

## 6. Модель компиляции шаблонов

Понимание этой части — ключ к чтению ошибок шаблонов.

### Двухфазный поиск имён

Шаблон проверяется в две фазы — это **[двухфазный поиск имён](../glossary.md#two-phase)** (two-phase name lookup):

1. **Первая фаза** — при чтении определения шаблона. Проверяется синтаксис, имена, **не зависящие** от шаблонных параметров.
2. **Вторая фаза** — при **инстанцировании**, когда компилятор знает конкретный `T`. Проверяются зависимые от `T` имена.

![Два момента на оси времени компиляции: при чтении определения проверено имя syntax_error, не зависящее от T, — CE здесь; при инстанцировании f<int>() проверено T::value — CE уже в месте использования](img/two-phase.svg)

Из этого следует контринтуитивный факт: **ошибки в шаблоне часто появляются не в файле, где он объявлен, а в файле, где он используется**. И конструкции «вверху ошибки» относятся к **месту использования**, потом к нескольким уровням инстанцирования, и только в самом низу — к строке в самом шаблоне.

Это и есть «template error spew» — простыни ошибок, которые так сложно читать. Концепты (C++20) сильно улучшают эту ситуацию.

### Шаблоны живут в заголовках

Чтобы инстанцировать шаблон, компилятор должен видеть **его определение** (не просто объявление, как у обычных функций).

![Две панели: у обычной функции в f.h только объявление, a.cpp и b.cpp тела не видят, тело лежит в отдельном f.cpp и его находит линковщик; у шаблона тело целиком в f.h, копия приезжает и в a.cpp, и в b.cpp, каждая единица трансляции инстанцирует сама, отдельного .cpp нет](img/template-in-header.svg)

Это означает, что **определения шаблонов** обычно живут **в заголовочных файлах**, не в `.cpp`. Альтернатива — **явное инстанцирование** (explicit instantiation), но она используется редко и в курсе не покрывается.

Прямое следствие — добавление шаблонов в заголовок увеличивает время компиляции **всех** включающих этот заголовок файлов. В больших проектах это серьёзная проблема. Современная попытка её решить — **модули** C++20, но они пока широко не поддерживаются.

## 7. Перегрузка шаблонов

[Перегрузка](../glossary.md#overload) (overloading) работает и для шаблонов: шаблонную функцию можно перегружать обычной, шаблонной с другими параметрами, и более специализированной:

```cpp
void f(int x)         { std::print("int\n"); }              // 1
template <typename T>
void f(T x)           { std::print("template T\n"); }        // 2
template <typename T>
void f(T* x)          { std::print("template T*\n"); }       // 3

f(42);          // 1: обычная функция предпочтительнее шаблона
f(42.0);        // 2: T = double
int* p = nullptr;
f(p);           // 3: T = int, более специализированный шаблон
```

Правила [разрешения перегрузки](../glossary.md#overload-resolution) (overload resolution) для шаблонов выстраивают кандидатов по приоритету — и первый подошедший сверху выигрывает; ничья на одном уровне даёт CE: [неоднозначный вызов](../glossary.md#ambiguous) (ambiguous call).

![Лесенка из трёх кандидатов: сверху не-шаблон void f(int), под ним более специализированный template void f(T*), внизу менее специализированный template void f(T); слева три вызова, розовые стрелки ведут f(42) к первому, f(p) ко второму, f(42.0) к третьему](img/overload-priority.svg)

`void f(T*)` более специализирован, чем `void f(T)`, потому что `T*` — это уже не любой `T`, а только указатель.

Эти правила — упрощённая версия разрешения перегрузки. Полный алгоритм — отдельная тема, которая всплывает редко.

## 8. Специализация шаблонов

### Полная специализация

Когда нужна **отдельная реализация для конкретных параметров** — это **[специализация](../glossary.md#specialization)** (specialization):

```cpp
template <typename T>
struct TypeName { static constexpr const char* name = "?"; };

template <>
struct TypeName<int> { static constexpr const char* name = "int"; };

template <>
struct TypeName<double> { static constexpr const char* name = "double"; };

TypeName<int>::name;       // "int"
TypeName<char>::name;      // "?"
```

Синтаксис `template <>` (пустые угловые скобки) означает «полная специализация» (explicit specialization). Внутри — указываем конкретное значение параметра в `<>` после имени класса.

Полная специализация работает и для **функций**, и для **классов**.

### Частичная специализация

**Только для классов!** Часть шаблонных параметров фиксируется, часть остаётся свободной — это и есть «частично»:

```cpp
template <typename T, typename U>
struct Encoder { /* основной шаблон */ };

// частичная: U зафиксирован как string, T всё ещё свободный
template <typename T>
struct Encoder<T, std::string> { /* T-to-string encoder */ };

// полная: оба параметра зафиксированы
template <>
struct Encoder<int, std::string> { /* int-to-string, specifically */ };
```

При использовании компилятор выбирает наиболее специализированную из подходящих специализаций:

![Три паттерна сужающимися полосами: широкий основной Encoder<T, U>, уже — частичная Encoder<T, std::string>, самая узкая — полная Encoder<int, std::string>; справа три обращения, розовые стрелки ведут Encoder<int, int> к основному, Encoder<double, std::string> к частичной, Encoder<int, std::string> к полной](img/specialization-order.svg)

«Более специализированный» — это отношение «один паттерн строго уже другого»; выбор по нему называется частичным упорядочиванием (partial ordering). Сравниваются не числа зафиксированных параметров, а сами паттерны: аргументы одной специализации обязаны подставляться в другую, но не наоборот. Полная специализация всегда точнее любой частичной. Если из подошедших ни одна не уже остальных — CE из-за неоднозначности; если не подошла ни одна, берётся **[основной шаблон](../glossary.md#primary-template)** (primary template).

Через эти паттерны строится **большая часть type traits**, **[SFINAE](../glossary.md#sfinae)**, многие фичи стандартной библиотеки. Писать такое с нуля в курсе не понадобится, а узнавать в чужом коде — да.

Пример с одним параметром тоже работает и часто встречается:

```cpp
template <typename T>
struct IsPointer { static constexpr bool value = false; };

template <typename T>
struct IsPointer<T*> { static constexpr bool value = true; };
```

Здесь `T*` — это паттерн «указатель на что-то». Если шаблонный аргумент имеет такую форму — выбирается специализация, где `T` — это «то, на что указатель».

### Для функций — только полная специализация

```cpp
template <typename T>
T abs_val(T x);

template <>
int abs_val(int x);       // полная специализация для T = int

template <typename T>
T* abs_val(T* x);         // НЕ частичная специализация — новый шаблон-перегрузка
```

Третья строка **компилируется**, но это **не** частичная специализация (их для функций не существует), а **отдельный шаблон, который участвует в перегрузке** наравне с первым. Работает по правилам разрешения перегрузки: более специализированный шаблон побеждает менее (мы это видели в разделе 7).

Это правило плохо известно — многие думают, что синтаксис `T* abs_val(T*)` создаёт частичную специализацию первого шаблона. Это не так. Это новый шаблон, который участвует в разрешении перегрузки.

На практике для функций «частичная специализация» обычно не нужна — её роль играет перегрузка шаблонов, и она часто даже лучше: участвует в обычных правилах разрешения перегрузки и работает с разрешением по аргументам, а не по типу самого шаблона.

## 9. Шаблонные аргументы по умолчанию

Шаблонным параметрам можно дать значения по умолчанию:

```cpp
template <typename T = int>
struct Container {
    T value;
};

Container<> c1;            // T = int
Container<double> c2;      // T = double
```

Они могут зависеть от **предыдущих параметров**:

```cpp
template <typename T, typename Alloc = std::allocator<T>>
class vector { /* ... */ };
```

Так устроены практически все контейнеры [STL](../glossary.md#stl): `std::vector<T>` это на самом деле `std::vector<T, std::allocator<T>>`, а `std::map<K, V>` — `std::map<K, V, std::less<K>, std::allocator<std::pair<const K, V>>>`. Устройство [аллокаторов](../glossary.md#allocator) (allocators) в курс не входит.

## 10. Non-type template parameters

В шаблон можно передать не только тип, но и **значение** — такой параметр называется **[non-type template parameter](../glossary.md#non-type-parameter)**:

```cpp
template <typename T, size_t N>
class Array {
public:
    T data[N];
    constexpr size_t size() const { return N; }
};

Array<int, 5> a;       // массив из 5 int на стеке
```

Так устроен `std::array<T, N>`. `N` — это **число**, известное на этапе компиляции, и `Array<int, 5>` и `Array<int, 6>` — это **разные типы**.

### Что разрешено как non-type параметр

- Целочисленные типы: `int`, `size_t`, `bool`, `char`, любые из `<cstdint>`
- Указатели и ссылки на функции / глобальные объекты (с особенностями)
- `enum`-значения
- С C++20 — почти любой [structural type](../glossary.md#structural-type): в том числе пользовательские структуры, у которых все поля публичные и сами structural

```cpp
template <bool LoudErrors>
class Logger { /* ... */ };

Logger<true> verbose;     // одно инстанцирование
Logger<false> silent;     // другое
```

### Что не разрешено

- `float`, `double` — до C++20 нельзя; с C++20 можно, они тоже structural types
- `std::string` — нельзя как non-type параметр: это не structural type (у него приватные поля)
- Указатели на [временные объекты](../glossary.md#temporary) (temporary objects) и на объекты с automatic [storage duration](../glossary.md#storage-duration) — локальные переменные

Хороший приём — `bool` non-type параметры для compile-time переключения поведения класса. Это позволяет компилятору соптимизировать неактивную ветку до нуля.

## 11. Зависимые имена

В шаблонном коде часто возникает ситуация: имя зависит от `T` — это **[зависимое имя](../glossary.md#dependent-name)** (dependent name), — и компилятор в первой фазе **не знает**, что это — тип или значение.

```cpp
template <typename T>
void f() {
    T::value_type x;     // CE: parser doesn't know what T::value_type is
}
```

С точки зрения парсера, `T::value_type` это просто «что-то из `T`». Это может быть:

- Тип (`typedef int value_type;` или `using value_type = int;`)
- Статический член (`static int value_type;`)
- Шаблонный класс (`template <...> struct value_type;`)

Компилятор по умолчанию считает, что **это значение**, не тип. Поэтому `T::value_type x;` парсится как «умножение `T::value_type` на `x`» (как `value * x`) — что синтаксически неправильно.

### `typename` disambiguator

Чтобы сказать «это тип», используется ключевое слово `typename`:

```cpp
template <typename T>
void f() {
    typename T::value_type x;     // ok: parser теперь знает, что это тип
}
```

Это часто нужно при работе со стандартными контейнерами:

```cpp
template <typename Container>
void g(Container& c) {
    typename Container::iterator it = c.begin();
    // ...
}
```

Без `typename` будет CE «`Container::iterator` does not name a type».

С C++20 во многих случаях `typename` стал необязательным — компилятор научился сам распознавать, когда контекст требует тип. Но для кода, который должен компилироваться и старыми компиляторами, лучше оставлять.

### `template` disambiguator

Аналогичная проблема — когда зависимое имя это **шаблонный метод**:

```cpp
template <typename T>
void f(T t) {
    t.template foo<int>();    // ← template нужно, иначе CE
}
```

![Две панели: слева без template строка t.foo < int > () размечена как два оператора сравнения, что даёт CE; справа в t.template foo<int>() слово template помечено как подсказка парсеру, а foo<int>() — как вызов шаблонного метода](img/dependent-parse.svg)

Это та же история, что с `typename`: синтаксические подсказки парсеру.

### Правило

Если в шаблонном коде вы используете что-то из `T::...`, и это **тип** — пишите `typename`. Если это **шаблонный метод/класс** — пишите `template`. В обычном коде, где `T` известен, эти слова не нужны — но и не вредят.

## 12. Концепты — упоминание (C++20)

Главная боль шаблонов исторически — ошибки. Если код шаблона использует что-то, что у `T` не поддерживается, компилятор выдаёт огромную простыню из глубины инстанцирования.

```cpp
template <typename T>
T sum(const std::vector<T>& v) {
    T result{};
    for (auto x : v) result += x;
    return result;
}

struct NoAdd {};
sum<NoAdd>(...);   // ошибка простыней на 100+ строк
```

C++20 решает эту проблему через **концепты** — способ выразить требования к `T` явно:

```cpp
template <std::integral T>
T add(T a, T b) { return a + b; }
```

Если ограничение не выполнено — компилятор пишет читаемое сообщение: «`T` должен удовлетворять концепту `std::integral`».

Концепты — большая отдельная тема, и целиком она в курс не входит. Сейчас знайте, что эта возможность есть, и встретив `std::integral T` или `std::ranges::range R` в чьём-то коде — будете понимать, что происходит.

## 13. `constexpr` — упоминание

Шаблонные функции часто **вычислимы на этапе компиляции**, если их пометить `constexpr`:

```cpp
template <typename T>
constexpr T square(T x) { return x * x; }

constexpr int x = square(5);    // 25, вычислено компилятором
int y = square(42);              // тоже работает, в рантайме
```

`constexpr`-функция может выполняться на компиляции, если все аргументы — `constexpr`. Иначе работает в рантайме как обычная функция.

К C++20 значительная часть `<algorithm>`, `<numeric>` и даже `std::vector` стали `constexpr`. Это позволяет писать **на C++ программы, работающие во время компиляции** — `constexpr`-метапрограммирование.

Полный разбор `constexpr`, `consteval`, `constinit` и виртуальных `constexpr`-функций остаётся за рамками курса.

## Итоги

- **Шаблон** — инструкция компилятору, как генерировать код для каждого набора параметров. Это compile-time механизм.
- **Инстанцирование** — генерация конкретной функции/класса для конкретных параметров.
- **Шаблоны функций, классов, псевдонимов и переменных** — все эти формы есть в языке. Шаблоны псевдонимов (`using`) и шаблоны переменных (`constexpr T name`) активно используются в type traits.
- **Двухфазный поиск имён:** ошибки часто всплывают в месте использования, а не в месте определения шаблона.
- **Шаблоны живут в заголовках** — это следствие модели инстанцирования.
- **Перегрузка:** не-шаблон лучше шаблона, более специализированный шаблон лучше менее.
- **Специализация:** полная для функций и классов, частичная — **только для классов**. Для функций вместо частичной — перегрузка шаблонов.
- **Аргументы по умолчанию** работают и для шаблонов, могут зависеть от предыдущих.
- **Non-type template parameters** — целые типы и значения. Каждое значение → отдельный тип.
- **Зависимые имена:** `typename` если это тип, `template` если шаблонный метод.
- **Концепты (C++20)** делают ошибки шаблонов читаемыми. Подробный разбор — за рамками курса.
- **`constexpr`-шаблоны** — большая часть стандартной библиотеки сейчас `constexpr`.

## Что дальше

Следующая лекция — **[variadic templates](../glossary.md#variadic) и правила вывода типов**:

- Variadic templates ([пакеты параметров](../glossary.md#parameter-pack), parameter packs) — `template <typename... Ts>`
- **[Fold expressions](../glossary.md#fold)** (C++17) — выразительная операция над пакетом
- Применения в реальном коде: `make_unique`, `tuple`, `format`
- Правила вывода типов в шаблонах
