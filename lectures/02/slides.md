---
marp: true
theme: pt
footer: 'Программирование на языке C++. ВШПИ МФТИ 2026'
paginate: true
---
<style>
section {
    font-size: 25px;
}
</style>

<!-- _class: lead -->

# Лекция 2.

## Память и указатели

---

# План

1. Виды памяти
2. Указатели
3. `new` и `delete`
4. Односвязный список: узлы и указатели
5. Массивы и `sizeof`
6. RAII и умные указатели

---
<!-- header: 1. Виды памяти -->

# 1. Виды памяти

В C++ объекты живут в одной из трёх областей памяти:

1. **Стек** — для локальных переменных функций
2. **Статическая** — для глобальных и `static`-переменных
3. **Динамическая (куча, heap)** — временем жизни (lifetime) управляет программист

![width:780px](img/memory-map.svg)

---

## 1.1 Стек

Локальные переменные функций живут на стеке:

```cpp
void f() {
    int x = 5;        // x на стеке
    double y = 3.14;  // y тоже
}                     // в конце функции — x и y разрушаются
```

- Выделение и освобождение — **очень быстрые** (сдвиг указателя стека, stack pointer)
- Размер ограничен (обычно 1–8 МБ на поток)
- Время жизни **жёстко привязано** к области видимости (scope)
- Слишком глубокая рекурсия → **переполнение стека** (stack overflow)

---

## 1.2 Кадр стека (stack frame)

![width:920px](img/stack-frames.svg)

Вызов кладёт кадр на вершину стека, возврат снимает его целиком. Поэтому локальные переменные и живут ровно до конца своей функции — и поэтому возвращать адрес локальной переменной нельзя.

---

## 1.3 Статическая память

Глобальные переменные и `static`-переменные:

```cpp
int counter = 0;                   // глобальная — статическая память

void f() {
    static int call_count = 0;     // живёт между вызовами f
    ++call_count;
}
```

- Создаются **до** входа в `main`, разрушаются **после** выхода
- Размер фиксирован на этапе компиляции
- `static` внутри функции — **сохраняет значение между вызовами**

---

## 1.4 Динамическая память (heap / куча)

Управляется программистом через `new` / `delete` или умные указатели (smart pointers):

```cpp
int* p = new int(42);   // выделили память, p указывает на неё
// ... используем p
delete p;               // освободили
```

- Время жизни **не привязано** к области видимости
- Можно вернуть указатель из функции — объект переживёт return
- **Медленнее** стека: аллокатор (allocator) ищет блок, иногда делает системный вызов (system call)
- Если забыть `delete` — **утечка** (memory leak)

**Правило большого пальца:** берите стек, когда можно. Куча — когда нужно: большие объекты, динамический размер, объекты, переживающие функцию.

---
<!-- header: 2. Указатели -->

# 2. Указатели

**Указатель** — переменная, хранящая адрес другого объекта в памяти.

```cpp
int x = 42;
int* p = &x;      // p хранит адрес x
```

- `int*` — тип «указатель на `int`»
- `&x` — операция «взять адрес» (если `x` — lvalue)

Указатели — основа работы с памятью в C/C++ и базовый строительный блок для всех остальных абстракций (контейнеры, умные указатели, итераторы).

---

## 2.1 Байты и адреса

![width:900px](img/pointer.svg)

Память — одна лента байтов, у каждой строки свой адрес. `x` занимает четыре байта, `p` — восемь, и в этих восьми лежит адрес строки с `x`.

---

## 2.2 Разыменование

`*p` — операция, обратная `&`: «значение по адресу `p`».

```cpp
int x = 42;
int* p = &x;
std::print("{}\n", *p);   // 42

*p = 100;                 // меняем x через указатель
std::print("{}\n", x);    // 100
```

Если `p` — указатель на тип `T`, то `*p` — выражение типа `T&` (ссылка на `T`). Со ссылками подробнее в следующей лекции.

---

## 2.3 `nullptr`

Указатель может не указывать ни на что — тогда его значение `nullptr`:

```cpp
int* p = nullptr;

if (p == nullptr) {
    std::print("пусто\n");
}

if (p) {                  // эквивалентно p != nullptr
    *p = 5;
}
```

**Разыменовать `nullptr` — это UB.** Перед `*p` всегда убеждайтесь, что указатель валиден.

До C++11 для этого использовали `NULL` (макрос) или просто `0`. **В новом коде только `nullptr`** — он типобезопасный.

---

## 2.4 Адресная арифметика

К указателю можно прибавлять и вычитать целые числа:

```cpp
int arr[5] = {10, 20, 30, 40, 50};
int* p = arr;        // указатель на arr[0]

std::print("{}\n", *(p + 1));   // 20
std::print("{}\n", *(p + 2));   // 30
```

![width:760px](img/pointer-arith.svg)

Эта арифметика валидна **только внутри одного массива**. Выход за границы — UB.

---

## 2.5 `const` и указатели

Есть три разных вещи, и их легко путать:

![width:880px](img/const-pointer.svg)

Читается справа налево: `int* const p` — «p это const-указатель на int».

---

## 2.6 `void*`

`void*` — указатель «на что угодно». Не имеет типа, на него нельзя разыменовать.

```cpp
int x = 5;
void* p = &x;            // ok: любой указатель → void*

// std::print("{}\n", *p);   // CE: cannot dereference void*

int* px = static_cast<int*>(p);   // обратно через cast
std::print("{}\n", *px);           // 5
```

`void*` — наследие C, используется в системных API (`malloc` возвращает `void*`, `memcpy` принимает `void*`). В современном C++ обходится без него — есть шаблоны и `std::any`.

---
<!-- header: 3. new и delete -->

# 3. `new` и `delete`

`new T(args)` — выделить память в куче под объект типа `T` и сконструировать его.

```cpp
int* p = new int(42);
std::print("{}\n", *p);   // 42
delete p;                  // освобождаем
```

`delete p` — разрушить объект (вызвать деструктор) и освободить память.

Каждому `new` должен соответствовать ровно один `delete`. Иначе:

- Забыли `delete` → **утечка памяти**
- Вызвали `delete` дважды → **double free** (UB, обычно крэш)
- Используем `p` после `delete` → **use-after-free** (UB)

---

## 3.1 `new[]` и `delete[]` для массивов

Для динамических массивов есть отдельные формы:

```cpp
int* arr = new int[100];   // массив из 100 int
arr[0] = 1;
arr[99] = 2;
delete[] arr;              // именно delete[] !
```

**Важно:** `new T[n]` идёт в паре с `delete[]`, а `new T` — с `delete`. Перепутать — UB.

```cpp
int* a = new int[10];
delete a;                  // UB: должен быть delete[]
```

В современном коде вы редко пишете `new[]` руками — вместо этого `std::vector` (динамический массив) или `std::array` (статический).

---

## 3.2 Утечки памяти

```cpp
void f() {
    int* p = new int(42);
    if (something_bad) {
        return;            // утечка: delete не вызвался
    }
    delete p;
}
```

Любой ранний выход из функции (`return`, исключение) **до** `delete` — утечка.

Решение — **RAII** и умные указатели (про них — в разделе 6). Они освобождают память **автоматически** при выходе из области видимости.

---

## Почему утечка — это не «мелочь»

Короткоживущей утилите утечка почти безразлична: процесс завершился, ОС всё вернула.

Но агент защиты рабочей станции запускается при загрузке и живёт **месяцами**. Пусть он теряет 100 байт на каждом обработанном событии, а событий — 20 000 в секунду:

```
100 байт × 20 000 событий/с × 3600 с ≈ 7 ГБ в час
```

Через сутки машину нельзя использовать — и виноват в этом инструмент, который её защищает.

Долгоживущий процесс **не имеет права** течь. Чем это лечится — в разделе 6.

---

## 3.3 Что остаётся после delete

![width:920px](img/dangling.svg)

Освобождение трогает кучу, а не указатель. Из этого растут оба следующих подвоха.

---

## 3.4 Double free и use-after-free

```cpp
int* p = new int(42);
delete p;
delete p;                  // UB: double free
*p = 100;                  // UB: use-after-free
```

После `delete` объект уничтожен, а `p` хранит тот же адрес. Повторный `delete` освобождает уже освобождённое — крэш или, хуже, тихая порча кучи. Чтение или запись по адресу — UB.

Двойной `delete` обычно приходит из **двойного владения** (ownership): два указателя на один объект, и оба его удаляют. Use-after-free коварнее: иногда «работает», потому что память не успели переиспользовать, и баг всплывает через час работы.

**AddressSanitizer** (`-fsanitize=address`) находит оба надёжно.

---
<!-- header: 4. Односвязный список -->

# 4. Односвязный список: узлы и указатели

Цепочка узлов — простейший способ хранить растущую последовательность записей, например последние события, пришедшие с машины. Ровно такое окно последних событий — задание занятия 1.2.

**Узел** = значение + указатель на следующий узел. Список хранит только **голову**.

```cpp
struct Node {
    int value;
    Node* next;      // nullptr у последнего узла
};
```

---

## 4.1 Узлы в куче

![width:900px](img/list.svg)

Голова лежит на стеке, узлы — в куче, каждый по своему адресу. Связь между ними держит только поле `next`.

---

## 4.2 Зачем так хранить

Элементы **не лежат подряд**: каждый узел выделяется в куче отдельно, связь задаётся указателями.

Отсюда главное свойство: **вставка и удаление не двигают соседей** — достаточно переписать пару указателей.

- узлы остаются на своих адресах, уже взятые указатели на них не протухают
- у массива вставка в середину сдвигает весь хвост, а рост переселяет все элементы на новое место

За это придётся заплатить — про цену на последнем слайде раздела.

---

## 4.3 Сырая версия: `new` руками

```cpp
Node* push_back(Node* head, int value) {
    Node* node = new Node{value, nullptr};
    if (head == nullptr) {
        return node;              // список был пуст
    }
    Node* last = head;
    while (last->next != nullptr) {
        last = last->next;
    }
    last->next = node;
    return head;
}
```

Поиск хвоста линейный; в настоящей реализации хвост хранят отдельным указателем.

---

## 4.4 Обход и освобождение

```cpp
for (Node* it = head; it != nullptr; it = it->next) {
    std::print("{} ", it->value);
}

void clear(Node* head) {
    while (head != nullptr) {
        Node* next = head->next;   // сохранить ДО delete
        delete head;
        head = next;
    }
}
```

**Подвох:** читать `head->next` после `delete head` — use-after-free. Забыли `clear` (или вышли раньше по `return`) — утечка всей цепочки.

---

## 4.5 Порядок в цикле освобождения

![width:920px](img/clear-order.svg)

---

## 4.6 Цена

- **Отдельная аллокация на каждый элемент** плюс указатель в каждом узле: накладные расходы сравнимы с самими данными.
- **Обход прыгает по памяти** — узлы лежат где придётся. Массив на том же количестве элементов обходится в разы быстрее просто потому, что лежит подряд. Про локальность (locality) — на лекции 13.

В реальном коде берут `std::list` или, гораздо чаще, `std::vector`. Про стандартные контейнеры — лекция 13.

Свой список пишут ради понимания устройства и в специальных случаях: когда нужны стабильные указатели на элементы.

Владение здесь ручное. Автоматический вариант — в разделе 6.

---

<!-- header: 5. Массивы и sizeof -->

# 5. Массивы и `sizeof`

В C++ массивы бывают четырёх видов:

```cpp
int a[10];                  // C-style: на стеке, фиксированный размер
int* b = new int[n];        // динамический, на куче
std::array<int, 10> c;      // C++: обёртка над C-style
std::vector<int> d(n);      // C++: динамический массив, умеет расти
```

В современном C++ **массивы C-style использовать практически не нужно**: `std::array` и `std::vector` дают всё то же самое плюс безопасность и удобный интерфейс.

**Правило:** размер известен на этапе компиляции — `std::array`, иначе — `std::vector`. Подробности — на лекции 13.

---

## 5.1 Подвох C-style массивов

```cpp
void f(int arr[10]) {                       // выглядит как массив из 10
    std::print("size: {}\n", sizeof(arr));  // выведет 8 (sizeof(int*))!
}
```

При передаче в функцию C-array **распадается до указателя** (decay) — теряется информация о длине.

Поэтому в C ходят два аргумента `(int* arr, size_t n)`. По той же причине так устроены `os.h` и `edr.h` в практикуме: это границы на C, и длину они передают отдельным параметром.

Тип, который знает свой размер и при передаче его не теряет, в C++ есть — разберём на лекции 14.

---
## 5.2 `sizeof`

`sizeof` — оператор, который возвращает размер типа или объекта в байтах.

```cpp
std::print("{}\n", sizeof(int));     // 4 (обычно)
std::print("{}\n", sizeof(double));  // 8

int x = 5;
std::print("{}\n", sizeof(x));       // 4

std::vector<int> v(100);
std::print("{}\n", sizeof(v));       // ~24 (sizeof самого объекта vector, не данных!)
```

Возвращаемый тип — `size_t`. Значение известно **на этапе компиляции**.

---
<!-- header: 6. RAII и умные указатели -->

# 6. RAII и умные указатели

К концу этой лекции мы умеем работать с динамической памятью через `new` / `delete`. Проблема в том, что **писать это в реальном коде опасно**:

- забыть `delete` — утечка
- удалить дважды — крэш
- удалить и продолжить пользоваться — UB

Решение — переложить ответственность за освобождение **на компилятор**. Это называется **RAII**.

---

## 6.1 Идея RAII

**RAII = Resource Acquisition Is Initialization.**

Принцип: ресурс владеется объектом, а его деструктор автоматически вызывается при выходе из области видимости.

```cpp
{
    SomeResourceOwner r(...);   // конструктор захватил ресурс
    use(r);
} // выход из блока — деструктор r отдал ресурс обратно
```

Это работает для любых ресурсов: памяти, файловых дескрипторов, мьютексов (mutex), сетевых соединений. В случае памяти — реализация называется **умные указатели**.

---

## 6.2 `std::unique_ptr`

`std::unique_ptr<T>` — RAII-обёртка над одиночным указателем. Главная фишка: **сам вызывает `delete` в деструкторе**.

```cpp
#include <memory>

void f() {
    auto p = std::make_unique<int>(42);
    *p = 100;
    std::print("{}\n", *p);
}   // здесь p автоматически удалит свой int — никакого delete руками
```

`std::make_unique<T>(args...)` — стандартный способ создать `unique_ptr`. Аргументы передаются в конструктор `T`.

---

## 6.3 `unique_ptr` для классов

Работает с любым типом — собственным или из библиотеки:

```cpp
struct Point {
    int x, y;
    Point(int x, int y) : x(x), y(y) {}
};

auto p = std::make_unique<Point>(3, 4);
std::print("{} {}\n", p->x, p->y);   // 3 4
```

Доступ к полям и методам — через `->`, как у обычного указателя. К значению — через `*p`.

---

## 6.4 `unique_ptr` нельзя копировать

```cpp
auto p1 = std::make_unique<int>(42);
auto p2 = p1;                      // CE: cannot copy unique_ptr
```

Слово «unique» в названии серьёзное: **только один владелец** может существовать. Если нужно передать владение — есть `std::move`:

```cpp
auto p2 = std::move(p1);           // ok: p2 владеет, p1 теперь пустой
```

Подробно про `std::move` — на лекции 11 (move-семантика, move semantics). Сейчас просто запомните: `unique_ptr` нельзя копировать, можно перемещать через `std::move`.

---

## 6.5 Список сам себе RAII

Вернёмся к списку из раздела 4: сделаем владельцем **сам список**.

```cpp
struct List {
    Node* head = nullptr;
    ~List() {
        while (head != nullptr) {
            Node* next = head->next;   // адрес — ДО delete
            delete head;
            head = next;
        }
    }
};
```

`new`/`delete` не исчезли — они **собраны в одном типе**, отвечающем за парность. Забыть освобождение нельзя, пропустить при исключении — тоже.

Узлы сырые: владелец один, `next` показывает дорогу. Так устроены все контейнеры.

---

## 6.6 `std::shared_ptr` — и когда какой

Если **владеть объектом должны несколько мест** — нужен `std::shared_ptr<T>`: он считает ссылки и удаляет объект, когда последняя пропадает.

```cpp
auto a = std::make_shared<int>(42);
auto b = a;                        // ok: счётчик ссылок = 2
```

![width:620px](img/shared-count.svg)

`shared_ptr` стоит дороже: счётчик ссылок (reference count) и атомарные операции (atomic). **Правило:** начинайте с `unique_ptr`, меняйте на `shared_ptr`, только когда владельцев действительно несколько.

---

## 6.7 Правило в этом курсе

Дальше в практических заданиях **не пишите `new` и `delete` руками в обычном
коде**. Исключение одно: тип, который сам владеет памятью и возвращает её
в своём деструкторе. В практикуме такой один — окно событий занятия 1.2.
Снаружи используйте:

- `std::make_unique<T>(args)` вместо `new T(args)`
- `std::make_shared<T>(args)` для разделяемого владения
- `std::vector` вместо `new T[n]`
- `std::array` вместо `T arr[N]`

Это убирает целые классы багов (утечки, double free, use-after-free) **без потери производительности** — `unique_ptr` компилируется ровно в то же, что и ручной `new`/`delete`.

Как они устроены внутри — разберём в **лекции 15**.

---
<!-- header: Итоги -->

# Итоги лекции

- Память делится на **стек**, **статическую** область и **кучу** — у каждой свои правила времени жизни.
- **Указатель** — переменная с адресом. `&` берёт адрес, `*` разыменовывает. `nullptr` — нет значения.
- **`new` / `delete`** — куча вручную; подвохи: утечка, double free, use-after-free.
- **Односвязный список** — узел «значение + указатель»; цена вставки — аллокация на элемент.
- C-style массивы — на `std::array` / `std::vector`; **RAII** — ресурс освобождается в деструкторе владельца.
- **Контейнер владеет хранилищем сам**: его RAII — собственный деструктор, а узлы внутри сырые.
- В практикуме — умные указатели; `new`/`delete` — только внутри владельца.

---

# Дальше

Следующая лекция — **функции, ссылки, константы, касты**:

- Перегрузка функций (overloading)
- Ссылки `&` — что это и чем отличаются от указателей
- 4 способа передачи в функцию
- `const` детально
- Касты (casts): `static_cast`, `reinterpret_cast`, `const_cast`
- Как функция сообщает об ошибке — и почему RAII из этой лекции обязателен
