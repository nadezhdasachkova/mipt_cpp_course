# Замороженные границы форматированию не подлежат.
#
# os.h и edr.h свёрстаны руками: значения макросов выровнены в колонку,
# комментарии к элементам enum — тоже, вложенные #if сдвинуты. Это читается
# как таблица контракта, ради чего и сделано, и clang-format всё это разберёт
# обратно в ленту. Процедура правки этих файлов — раздел 11 DEVELOPMENT.md,
# и переформатирование в неё не входит.

DisableFormat: true
SortIncludes: Never
